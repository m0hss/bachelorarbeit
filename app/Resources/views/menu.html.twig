<div class="navbar-collapse collapse">
    <ul class="nav navbar-right">
        {% if is_granted('ROLE_CHEF_DEPARTEMENT') %}
            <li class="dropdown">
                <a href="{{ path('liste_demandes_conges') }}" class="dropdown-toggle">
                    <i class="fa fa-calendar-o"></i> Gestion de conge
                </a>

            </li>
            <li class="dropdown">
                <a href="{{ path('liste_demande_stage') }}" class="dropdown-toggle">
                    <i class="fa fa-university"></i> Gestioon de stage
                </a>
            </li>
        {% endif %}

        {% if is_granted('ROLE_ADMIN') %}
            <li class="dropdown">
                <a href="{{ path('admin_employe_liste') }}" class="dropdown-toggle">
                    <i class="fa fa-group"></i> Emplyoés
                </a>

            </li>
            <li class="dropdown">
                <a href="{{ path('admin_fonction_liste') }}" class="dropdown-toggle">
                    <i class="fa fa-list"></i> Fonctions
                </a>
            </li>
            <li class="dropdown">
                <a href="{{ path('admin_type_conge_liste') }}" class="dropdown-toggle">
                    <i class="fa fa-calendar-o" ></i> Types conge
                </a>
            </li>
            <li class="dropdown">
                <a href="{{ path('admin_departement_liste') }}" class="dropdown-toggle">
                    <i class="fa fa-building"></i>
                    Départements
                </a>
            </li>

        {% endif %}

        {% if is_granted('ROLE_ADMIN') == false and is_granted('ROLE_CHEF_DEPARTEMENT')  == false %}

            <li class="dropdown">
                <a href="{{ path('admin_mes_conges') }}" class="dropdown-toggle">
                    <i class="fa fa-calendar-o"></i>
                    <span class="badge badge-danger"> 23 </span>
                    Mes conges
                </a>
            </li>

        {% endif %}


        <li class="dropdown">
            <a href="{{ path('fos_user_security_logout') }}" class="dropdown-toggle">
                <i class="fa fa-power-off"></i> Déconnexion
            </a>
        </li>

        {% if app.user %}
        <!-- start: USER OPTIONS DROPDOWN -->
        <li class="dropdown current-user">
            <a href="{{ path('fos_user_profile_show') }}">
                {% if app.user.image %}
                    <img src="{{ asset(app.user.image.webPath) }}" alt="" width="39" height="39">
                {% elseif app.user.image == null and app.user.sexe == "Homme" %}
                    <img src="{{ asset('images/homme.png') }}" alt="" width="39" height="39">
                {% elseif app.user.image == null and app.user.sexe == "Femme" %}
                    <img src="{{ asset('images/femme.png') }}" alt="" width="39" height="39">
                {% endif %}

                <span class="username">{{ app.user.prenom }} </span>
            </a>
        </li>
        <!-- end: USER OPTIONS DROPDOWN -->
        {% endif %}
    </ul>
    <!-- start: MENU TOGGLER FOR MOBILE DEVICES -->
    <div class="close-handle visible-xs-block menu-toggler" data-toggle="collapse" href=".navbar-collapse">
        <div class="arrow-left"></div>
        <div class="arrow-right"></div>
    </div>
    <!-- end: MENU TOGGLER FOR MOBILE DEVICES -->
</div>